{
  "name": "@docsgraph/search",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@docsgraph/data": "workspace:*"
  },
  "devDependencies": {
    "@docsgraph/config": "workspace:*",
    "eslint": "^9.15.0",
    "typescript": "^5.7.3",
    "vitest": "^2.1.9"
  }
}
